<?xml version="1.0" encoding="UTF-8"?>
<project name="adito-application-jica" default="dist">

        <!-- Standard build locations -->
        <property name="build.src" value="src" />
        <property name="output.file" value="adito-application-jica.zip" />

	<!-- Build the adito-application-jica.zip -->
	<target name="dist">
		<zip destfile="${output.file}">
			<zipfileset dir="${build.src}" prefix="adito-application-jica">
			</zipfileset>
		</zip>
	</target>

</project>
